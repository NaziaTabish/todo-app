"""Package entry point for todo application.

This module allows running the application with 'python -m src' instead of
requiring direct path manipulation.
"""

from cli.main import main

if __name__ == "__main__":
    main()
